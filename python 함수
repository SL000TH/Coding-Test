1. 사전 자료형 (키, 값)
 1) keys() : 키 데이터만 뽑아서 리스트
 2) values() : 값 데이터만 뽑아서 리스트 

2. 집합 자료형 : 중복X , 순서X
 1)합집합 : | (or)
 2)교집합 : & (and)
 3)차집합 : a-b 
 4)새로운 원소 추가 : add
 5)새로운 원소 여러개 추가 : update
 6)특정한 원소 제거 : remove

3. 기본 입출력
 1)input() : 한줄의 문자열을 입력 받는 함수
 2) map() : 리스트의 모든 원소에 각각 특정한 함수를 적용할 때 사용합니다. 
 3) sys.stdin.readline().rstrip : 입력을 최대한 빠르게 받음 
 4) print(f"정답은 {answer}입니다.")

4. 자주 사용되는 내장 함수 
 1) sorted() with key
  - sorted(array, key=lamda x:x[1], reverse = True)

2)nCr(combination), nPr(permutations), product(중복순열), combination_with_replacement(중복 조합)
 - from intertools import combination, permutations, product,combination_with_replacement
 
3) 등장 횟수 세기
 -from collections import Counter

4) 최대공약수,최소공배수
 -import math
      gcd()최대공약수 lcm()최소공배수
